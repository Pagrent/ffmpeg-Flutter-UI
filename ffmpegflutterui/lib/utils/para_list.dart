import 'package:flutter/material.dart';

class YoKi extends StatelessWidget {
  List<String> FileList;
  int selectedFunction;

  YoKi({
    super.key,
    required this.FileList,
    required this.selectedFunction,
  });

  @override
  Widget build(BuildContext context) {
    
    return Placeholder(

    );
  }
}